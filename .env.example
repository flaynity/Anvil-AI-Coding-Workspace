PORT=8787
# Comma-separated production origins. Use * only for testing.
ALLOWED_ORIGINS=*
# Set true only if you intentionally need localhost/private provider targets.
ALLOW_PRIVATE_TARGETS=false
PROVIDER_TIMEOUT_MS=120000
JSON_LIMIT=8mb
